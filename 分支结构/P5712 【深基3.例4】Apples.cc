#include <bits/stdc++.h>
using namespace std;
int main(){
    int a;
    cin>>a;
    if (a<=1)
    {
        printf("Today, I ate %d apple.",a);
    }else if (a>1)
    {
        printf("Today, I ate %d apples.",a);
    }
    
    
}
